# Local machine paths (copy from scripts/abm_paths.local.sh.example)
scripts/abm_paths.local.sh

# External ABM4bio install (not vendored in this repo)
ABM4bio/

# Python bytecode
__pycache__/
*.pyc

# Optuna study databases and run artifacts (regenerate via executables/*/optuna_*.sh)
outputs/optuna/EGI1/control/abm_evals
outputs/optuna/studies/*.db